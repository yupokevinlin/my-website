export enum ThemePictureSeason {
  SPRING = "Spring",
  SUMMER = "Summer",
  AUTUMN = "Autumn",
  WINTER = "Winter",
}

export interface ThemePictureLoad {
  spring: boolean;
  summer: boolean;
  autumn: boolean;
  winter: boolean;
}
